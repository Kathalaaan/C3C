(()=>{const c=window.ROADMAP_DATA;if(!c)return;const root=document.getElementById('roadmap'),info=document.getElementById('info');document.title=c.subject+' Roadmap';document.querySelector('h1').textContent=c.subject;document.getElementById('subcopy').textContent=c.description;document.getElementById('basis').textContent=c.basis;root.style.setProperty('--roadmap-height',c.height+'px');const inner=root.querySelector('.roadmap-inner');inner.insertAdjacentHTML('beforeend','<svg class="lines" viewBox="0 0 1240 '+c.height+'"></svg><div class="legend"><strong>Quick Legend</strong><span>Yellow boxes are milestones. Cream boxes are topic cards. Click any node to open the study brief and revision cue.</span></div>');const svg=inner.querySelector('.lines'),map={};for(const n of c.nodes){const b=document.createElement('button');b.className='node'+(n.kind==='m'?' m':'');b.style.left=n.x+'px';b.style.top=n.y+'px';b.style.width=(n.w||360)+'px';b.innerHTML='<h3>'+n.t+'</h3>'+(n.s?'<p>'+n.s+'</p>':'');b.onclick=()=>show(n.id);inner.appendChild(b);map[n.id]=b}function drawLine(a,b,d){a=map[a];b=map[b];if(!a||!b)return;const ar=a.getBoundingClientRect(),br=b.getBoundingClientRect(),sr=svg.getBoundingClientRect(),sx=1240/sr.width,sy=c.height/sr.height;let x1=ar.left+ar.width/2,y1=ar.top+ar.height/2,x2=br.left+br.width/2,y2=br.top+br.height/2;if(Math.abs(x2-x1)>=Math.abs(y2-y1)){x1=x2>x1?ar.right:ar.left;x2=x2>x1?br.left:br.right}else{y1=y2>y1?ar.bottom:ar.top;y2=y2>y1?br.top:br.bottom}x1=(x1-sr.left)*sx;y1=(y1-sr.top)*sy;x2=(x2-sr.left)*sx;y2=(y2-sr.top)*sy;const mid=Math.abs(x2-x1)>=Math.abs(y2-y1),dstr=mid?`M ${x1} ${y1} C ${(x1+x2)/2} ${y1}, ${(x1+x2)/2} ${y2}, ${x2} ${y2}`:`M ${x1} ${y1} C ${x1} ${(y1+y2)/2}, ${x2} ${(y1+y2)/2}, ${x2} ${y2}`;const p=document.createElementNS('http://www.w3.org/2000/svg','path');p.setAttribute('d',dstr);if(d)p.setAttribute('class','d');svg.appendChild(p)}function draw(){svg.innerHTML='';c.links.solid.forEach(x=>drawLine(x[0],x[1],0));c.links.dashed.forEach(x=>drawLine(x[0],x[1],1))}function show(id){const base=c.nodes.find(n=>n.id===id)||c.nodes[0],n=c.details[id]||{t:base.t,w:base.s||'Use this node to strengthen the surrounding milestone before moving ahead.',g:base.kind==='m'?'Milestone block':'Topic-level scoring support.',r:'Revise this node with one clean format, one key concept list, and one timed question.',p:['Cover the full chapter flow once.','Note recurring traps or formulas.','Reconnect it to the milestone above.']};info.innerHTML='<h2>'+n.t+'</h2><div class="k">Why It Matters</div><p>'+n.w+'</p><div class="k">Weightage Cue</div><p>'+n.g+'</p><div class="k">Revision Tip</div><p>'+n.r+'</p><div class="k">Priority Points</div><ul>'+n.p.map(x=>'<li>'+x+'</li>').join('')+'</ul>'}function scale(){const s=Math.min(1,root.clientWidth/1240);root.style.setProperty('--scale',s);root.style.height=Math.ceil(c.height*s)+'px';requestAnimationFrame(draw)}window.addEventListener('resize',scale);window.addEventListener('load',scale);if(document.fonts?.ready)document.fonts.ready.then(scale);show('start');scale()})();

